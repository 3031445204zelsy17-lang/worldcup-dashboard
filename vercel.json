{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "${AZURE_API_URL}/api/:path*" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
